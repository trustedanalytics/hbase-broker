security:
  user.password: fake
hbase:
  brokergroupname: supergroup
  provided.zip: ${hadoop.provided.zip:}
store:
  path: ${broker.store.znode:/broker/hbase}
cf:
  servicename: ${cf.catalog.servicename:hbase}
  serviceid: ${cf.catalog.serviceid:hbase}
  baseId: ${BASE_GUID:fake}
metadata:
  imageUrl: ${IMAGE_URL:}
